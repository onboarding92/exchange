# Example Dockerfile for BitChange frontend (Vite + React)
# Build step + Nginx serve. Adjust paths as needed.

FROM node:20-alpine AS build

WORKDIR /app
COPY client/package.json client/package-lock.json* ./
RUN npm install

COPY client/ ./
RUN npm run build

# --- Static server stage ---
FROM nginx:alpine

# Copy built assets
COPY --from=build /app/dist /usr/share/nginx/html

# Default Nginx config is ok for a SPA; you may want to tweak for history mode.
EXPOSE 80
