# Example Dockerfile for BitChange backend (TypeScript, Node 20)
# This is a starting point; adjust to your build/run scripts.

FROM node:20-alpine

WORKDIR /app

# Install deps
COPY server/package.json server/package-lock.json* ./ 
RUN npm install --omit=dev

# Copy source
COPY server/ ./ 

# Expose port (must match PORT in .env)
EXPOSE 3000

# Start script (adjust if you later add a build step)
# For example, if you add "start": "node dist/index.js" and a build:
#   RUN npm run build
#   CMD ["npm", "run", "start"]
CMD ["npm", "run", "dev"]
