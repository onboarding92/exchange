# server/.env.production

# --- Core server config ---
PORT=3001
SESSION_SECRET=change_me_to_a_long_random_string_123!@#xyz

# Frontend origin in production (change with your real domain)
CORS_ORIGIN=https://app.yourdomain.com

# SQLite DB file path (relative to server working dir)
# In Docker we will map a volume so this file is persisted.
DATABASE_FILE=exchange.db

# --- SMTP config (email alerts / password reset / login alerts) ---
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=REPLACE_WITH_REAL_API_KEY
SMTP_FROM="BitChange <noreply@yourdomain.com>"

# --- 2FA / Security ---
TOTP_ISSUER=BitChange

# --- Optional rate limiting overrides (backend has defaults) ---
GLOBAL_RATE_WINDOW_MS=900000
GLOBAL_RATE_MAX=300
LOGIN_RATE_WINDOW_MS=300000
LOGIN_RATE_MAX=20
